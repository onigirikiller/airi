# AUTONOMY_BENCHMARKS

| Stage | Check | Status | Notes |
| --- | --- | --- | --- |
| 0 Infra | Canonical inventory/state present | PASS | Canonical inventory snapshot, slot-aware facts, capability handshake, blocked-step memory are in place. |
| 0 Infra | Unsupported capability blind retry removed | PASS | `compactInventory` unsupported path is cached and suppressed. |
| 0 Infra | Progress watchdog and deterministic recovery | PASS | Coordinate stall triggers queued deterministic recovery. |
| 0 Infra | Prompt/state exposes shaft-trap recovery context | PASS | Canonical world-state now emits `mobility_state` plus scaffold availability, and the recovery evaluator can promote a shaft-specific surface objective instead of only a generic underground fallback. |
| 0 Infra | Fabric bridge late-start tolerance | PASS | `FabricBridge.asyncBuild` now continues in deferred-connect mode, and the live daemon will start the soak supervisor without relaunching an already-open Minecraft gameplay window just because the bridge listener is late. |
| 0 Infra | Live daemon survives local launcher restrictions | PASS | `stream-gemma-daemon-worker.ps1` now degrades past `Win32_Process` access denial, uses a direct TCP bridge probe, bypasses corepack/pnpm, and reaches compiled supervisor launch before the remaining outer `spawn EPERM` sandbox boundary. |
| 0 Infra | Empty inventory regressions are short-lived guarded | PASS | `BotProxy` now preserves the last visible inventory for a short grace window when Fabric bridge `getInventory` / `state:inventory` snapshots collapse to `items=[]`, and it now persists the last visible inventory/armor/offhand snapshot across bot restarts so a short runtime restart does not immediately regress to empty bootstrap facts. |
| 0 Infra | Focused screen-copy capture path exists | PASS | `capture-minecraft-window.ps1` now supports `-PreferScreenCopy` so runs can bypass stale `PrintWindow` captures when foreground copying is more reliable. |
| 0 Infra | Remote walk watchdog rejects wall-jitter stalls | PASS | `remoteMoveToward` now gives up when position jitters without meaningful distance/elevation improvement, which let the latest live search action fall through into wood gathering instead of hanging on a stone-wall walk loop. |
| 0 Infra | Surface-recovery bridge waits obey interactive time budgets | PASS | `findBlocksAsync` no longer burns the default timeout on every queried block type, confined above-`y60` relocation now reuses the underground timeout, and the initial Fabric bridge `goto`/`gotoNear` request now honors `goal.timeoutMs` instead of always waiting 120s before fallback. |
| 0 Infra | Surface-recovery stage tracing localizes near-surface shaft failures | PASS | The April 19, 2026 compiled soak now enters an immediate shaft-trap pillar fast path before deeper cue/exit scans, and the latest live stop is explicitly reported as `head=unknown` in the pillar safety gate rather than an opaque deep `escapeTowardSurface` stall. |
| 1 Early survival | Stable fallback objectives without blind craft loop | PASS | Live reruns now clear the old wood/bootstrap loop: `birch_log -> birch_planks -> crafting_table` was verified in-world. |
| 1 Early survival | Japanese wood acquisition remains a resource goal | PASS | Explicit Japanese wood/log goals now require wood inventory or wood progress instead of verifying from downstream craft artifacts such as a nearby crafting table. |
| 1 Early survival | Underground cave-prep failures pivot back to surface recovery | PASS | Objective fallback and deterministic planner fast path redirect underground bootstrap / cave-prep failures to `Escape to the surface to gather wood`, planner-backed execution can call `recoverTowardSurface` directly, and the latest live compiled run now opens with `searchForBlock(grass_block) -> moveAway` before any blind local climb fallback. |
| 1 Early survival | Local surface cues preempt long recovery climbs | PASS | `services/minecraft/src/agents/planning/index.ts` now inserts `searchForBlock(grass_block) -> moveAway` ahead of `recoverTowardSurface` when nearby surface-facing blocks are already visible, and `services/minecraft/src/runner/phases.ts` now tries the same cue-driven handoff before deeper escape logic. |
| 1 Early survival | Capability-limited inventory cleanup does not hijack bootstrap | PASS | When both `compactInventory` and `swapInventorySlots` are unsupported, inventory cleanup urgency now falls below crafting-table / pickaxe bootstrap and `quick-loot` verification no longer fails on the `loot` substring. |
| 1 Early survival | Crafted-item goal verification survives bridge-lag | PASS | Planner verification now counts canonical inventory, held/offhand/equipped gear, nearby crafting-table / furnace access, and recent `inventory_sync_mismatch` craft diagnostics before retrying crafted goals; the latest live compiled soak verified the furnace milestone instead of falling back into the old craft loop. |
| 1 Early survival | Torch/fuel shortage reuses a structured charcoal-smelting path | PASS | When logs and a furnace already exist but coal is absent, objectives now emit `Smelt charcoal using logs for torch and furnace fuel`; the April 19, 2026 live compiled soak executed that exact goal text, retried transient `Furnace screen did not open` misses, and still verified `charcoal x1` in inventory. |
| 1 Early survival | Underground food recovery prefers surface-first resupply | PASS | `Collect nearby food` deterministic planning/recovery now switches to `recoverTowardSurface` when the bot is trapped underground near a visible surface cue, and the April 19, 2026 compiled soak immediately selected `Escape to the surface to gather wood` from the live `stone_pickaxe + furnace + charcoal + no food/no sword/no torches` state. |
| 1 Early survival | Food-hunt combat preflight matches executor fallback tools | PASS | Attack inventory readiness now accepts the same fallback melee tools (`pickaxe`, `shovel`) as `skills/combat.ts`, and the structured `Collect nearby food` fast path now prepends `craftRecipe(sword)` when current wood/crafting access makes that upgrade plausible. |
| 1 Early survival | Surface-cue ascent preserves gained altitude and reaches daylight | PASS | The April 19, 2026 live compiled soak from `(-407, 64, 235)` rose only `+1.25Y` on the first `grass_block` cue contact, then used the new exact cue-top ascent to `(-406, 69, 236)`, verified `terrainContext=surface_forest`, and continued directly into `collectBlocks(log)` / `oak_log x4` instead of dropping back into `moveAway` underground. |
| 1 Early survival | Stone -> furnace -> iron trajectory remains available | PASS | Objective framework still proposes furnace / smelt / iron mining paths. |
| 1 Early survival | Coastal food fallback does not depend only on land animals | PASS | Deterministic food supply now includes `cod` / `salmon`, and planning suppression treats fish searches as the same generic `food-animal` family as other survival hunts. |
| 1 Early survival | Localized food-target observations preserve canonical entity types | PASS | Action snapshots now retain both localized nearby names and canonical types (`タラ`, `minecraft:cod`), so generic `searchForEntity(animal)` verification no longer false-fails in fish-only coastal scenes. |
| 1 Early survival | Generic food combat can prefer land prey and swim after fish | PASS | `attackNearest('animal')` now deprioritizes aquatic targets when easier land animals are visible, and fish hunts request a short swim-mode pursuit plus direct swing retries when water prey is unavoidable. |
| 1 Early survival | Wood bootstrap can start bare-handed and repopulate inventory | PASS | The latest direct compiled soak (`runtime/manual-bot-start/20260419-072233`) moved off the old wall-stall, marked `inventory_task_readiness: mine=ready` for `collectBlocks(log)`, entered `gatherWood`, and a post-soak bridge probe reported `oak_log x3` in slot 0. |
| 1 Early survival | Structured wooden-pickaxe bootstrap respects shared plank inputs | PASS | The April 19, 2026 surfaced reattach (`services/minecraft/runtime/manual-compiled-soak/20260419-172645`) now live-verifies `Craft a wooden pickaxe` after reserving shared plank inputs correctly in the structured planner shadow; the paired screenshot `runtime/minecraft-observation/minecraft-window-20260419-172807.png` shows the wooden pickaxe equipped. |
| 1 Early survival | Wood search normalizes canopy hits into trunk-base approach points | PASS | `searchForBlock(log)` now routes through `approachNearestWoodTarget(...)`; the April 19, 2026 compiled soak (`services/minecraft/runtime/manual-compiled-soak/20260419-190750`) pathfound to `(-316, 64, 359), range=2` for the later wood search and still reached a verified `wooden_pickaxe x1` in the same window instead of climbing to a canopy Y target first. |
| 1 Early survival | Surface bootstrap objective continuity now prefers local stone-tier progress over stale cleanup/recovery churn | PASS | In the April 19, 2026 resumed surface/bootstrap reattach (`services/minecraft/runtime/manual-compiled-soak/20260419-201218`), autonomy selected `Craft a crafting table` and then `Craft a wooden pickaxe` instead of reopening `Escape to the surface to gather wood` or `Organize inventory...`; the objective layer now delays early cleanup and surfaces `Mine 16 cobblestone` / `Craft a stone pickaxe` as the next milestone once wooden tools exist. |
| 1 Early survival | Canopy-only wood detections no longer trap bootstrap in exact vertical ascent loops | PASS | `gatherWood` now skips unresolved elevated log-only candidates after horizontal approach instead of climbing toward the same high Y target. The April 19, 2026 live reattach (`services/minecraft/runtime/manual-compiled-soak/20260419-201728`) repeatedly logs `Skipping unresolved elevated wood candidate after horizontal approach`, proving the old ascent loop is cut short even though this terrain still needs better reachable-trunk selection to finish stone bootstrap quickly. |
| 1 Early survival | Canopy-heavy wood scans now diversify across distinct tree candidates | PASS | `gather-wood` now clusters nearby canopy observations before ranking and expands the scan when the first 24 hits collapse into too few distinct trees. In the April 19, 2026 live reattach (`services/minecraft/runtime/manual-compiled-soak/20260419-210921`), the same `Craft a wooden pickaxe` restart no longer burned retries on near-duplicate canopy coordinates; it skipped `(-297, 73, 188)` and `(-321, 73, 138)` once each, then relocated to a third candidate `(-324, 64, 178)` and entered vertical approach instead of immediately revisiting the prior canopy pair. |
| 1 Early survival | Wood search can hand off on horizontal trunk contact before canopy ascent | PASS | In `services/minecraft/runtime/manual-compiled-soak/20260419-220459`, `searchForBlock(log)` now logs `Accepting horizontal wood-candidate contact...`, verifies successfully, and proceeds into `collectBlocks(log)` instead of timing out on exact canopy Y alignment. |
| 1 Early survival | Wood collection now prefers reachable trunk breaking before generic `mine(log)` fallback | PASS | In `services/minecraft/runtime/manual-compiled-soak/20260419-221423`, the post-search collection path climbed to the cached trunk and manually broke `minecraft:oak_log` at `(-382,72,100)` / `(-382,73,100)` before continuing into `oak_planks -> stick -> wooden_pickaxe`, proving the new ordering avoids burning the whole bootstrap on early Baritone canopy blacklists. |
| 1 Early survival | Generic `craftRecipe(sword)` now survives inventory preflight and reaches ensure-managed crafting | PASS | In `services/minecraft/runtime/manual-compiled-soak/20260419-221423`, the recovery-replaced goal `Gather materials and craft a sword` reports `inventory_result=ready` for `craftRecipe(sword)`, then `ensureSword()` reuses the nearby crafting table and verifies `wooden_sword x1` in inventory. |
| 1 Early survival | Tree-cover surface scenes no longer self-classify as underground food recovery | PASS | `services/minecraft/src/agents/planning/index.ts` now suppresses the underground `recoverTowardSurface` food fast path when a nearby sky window is visible through natural canopy cover. The new regression is in place; the next live soak still needs to hit the food branch directly from this state. |
| 1 Early survival | Generic temporary shelter fallback uses actually held blocks | PASS | `services/minecraft/src/agents/planning/adapter.ts` now chooses `dirt`, planks, logs, or cobble-like blocks from the current inventory for generic shelter goals instead of assuming `cobblestone`. The new regression covers the dirt case; live verification is still pending the next shelter branch. |
| 1 Early survival | Post-death inventory state cannot leak stale tools into bootstrap | PASS | `BotProxy` now clears strict/raw inventory, armor/offhand, selected slot, snapshots, canonical inventory, and persisted resume state on death signals so a restart after death cannot preserve pre-death gear. |
| 1 Early survival | Already-surface recovery can complete without forced movement | PASS | `recoverTowardSurface` now returns/verifies success when world-state already reports a surface condition; the May 10, 2026 supervisor run moved past the prior surface-recovery no-op failure and continued into wood/tool bootstrap. |
| 1 Early survival | Unsafe wood search can defer into trunk-aware collection | PASS | Wood `searchForBlock` now returns an explicit defer result when trunk-aware targeting refuses unresolved canopy hits, and action verification accepts only that wood-specific result. Focused tests cover both notable and empty-observation defers. |
| 1 Early survival | Current Fabric state reaches wooden pickaxe from wood recovery | PASS | `services/minecraft/runtime/soak-supervisor/run-2026-05-09T17-10-25-883Z.log` live-verifies `collectBlocks(log)` gaining `oak_log x1`, then `oak_planks`, `stick`, and `wooden_pickaxe x1` with action verification passing. |
| 1 Early survival | Current Fabric state reaches stone tools and furnace craft | PASS | `services/minecraft/runtime/soak-supervisor/run-2026-05-09T21-06-57-739Z.log` live-verifies wood recovery continuing through `cobblestone x17`, `stone_pickaxe`, and `furnace`. |
| 1 Early survival | Furnace placement truth is checked after equip | PASS | `placeBlock` now refuses wrong-hand placement and confirms target block or requested inventory decrement before recording recent placement; BotProxy held-item state now uses canonical selected raw hotbar state. The next live restart must verify the real furnace GUI path. |
| 1 Early survival | Furnace/torch recovery survives wrong-hand placement | PASS | The May 10, 2026 supervisor restart failed `placeHere(furnace)` instead of accepting a dirt placement, then completed `smeltItem(oak_log) -> stick -> torch` through a real nearby furnace. Evidence: monitor API events and `runtime/screenshots/monitor-20260510-062806.png`. |
| 1 Early survival | Post-pickaxe stone/iron mining is survival-safe | FAIL | The same May 10, 2026 restart later selected `mine iron_ore at (-382, 44, 191)` at `health=12`, `food=6`, and moved underground before food recovery. Code-level precondition/objective gates now block this class; live validation is pending the next restart. |
| 2 Midgame competence | Base/home gap visible to planner | PASS | Canonical world-state now emits base/interior status and decision prompts carry objective gap facts. |
| 2 Midgame competence | Inventory/equip stable under pressure | PASS | Live reruns showed discard-based relief and organizer progress even when `compactInventory` / slot swap commands were unsupported by the Fabric bridge. |
| 3 Long-horizon autonomy | Fallback is target/gap driven, not fixed roadmap | PASS | `decision-provider.ts` now uses evaluator-driven objective proposals instead of the dragon roadmap fallback. |
| 4 Full diamond objective | Diamond path is gated by real mining capability | PASS | `pickaxeTier` now prevents iron-sword false positives, progression extends through `iron-pickaxe -> diamond-acquisition -> diamond-loadout`, and structured recipes cover diamond armor pieces. |
| 4 Full diamond objective | Full diamond armor reached in long soak | FAIL | No verified long soak pass yet. The May 10, 2026 supervisor run now proves the current Fabric state can reach `wooden_pickaxe x1` after safe canopy-aware wood recovery, then selects `Mine 16 cobblestone`. The remaining blocker has shifted to post-pickaxe survival safety: food/surface buffering and safe stone/iron handoff must stabilize before diamond mining can be treated as reachable. |
| 5 House build | Safe functional home built and validated | FAIL | Base evaluator exists, but build compiler/validator is not done in this iteration. |
| 6 Interior | Interior zones and decor pass | FAIL | Interior evaluator exists, compiler/placement path still pending. |
| 7 Stream quality | Speech never blocks gameplay-critical work | FAIL | Gameplay-critical actions kept running during sandbox `fetch` / local TTS failures, but a real non-sandbox soak is still needed before calling the speech lane safe. |

## Exit Criteria Snapshot

- Current known gameplay regressions green: `FAIL (wood bootstrap, pickaxe-tier gating, underground cave-prep rewrite logic, capability-limited inventory cleanup during bootstrap, the first-step surface recovery ordering, restart-time inventory resume/death clearing, bare-hand log bootstrap, remote walk no-progress watchdog, crafted-item goal verification after bridge-lag, charcoal objective routing, transient furnace-open recovery, the first underground food-recovery surface retarget, cue-contact altitude loss during surface escape, no-sword food-hunt preflight mismatch, the obvious 30-120s bridge/path dispatch waits inside confined shaft recovery, the opaque deep \`escapeTowardSurface\` stall, the structured wooden-pickaxe shared-plank double-spend, the localized fish-only search verification gap, the generic fish-pursuit combat stall, the old canopy-first wood search, the stale surfaced cleanup/recovery churn, the exact-Y canopy ascent loop, the worst same-canopy duplicate wood retries, the search-phase canopy-Y handoff bug, the unsafe wood-search verification failure, the early `mine(log)` canopy-blacklist ordering, generic sword-preflight rejection, Japanese wood-goal craft false positives, scaffold pickup loss after elevated wood recovery, furnace wrong-hand placement false positives, and low-hunger mining precondition gaps now have code-level fixes; the remaining live blockers are live validation of low-hunger food/surface recovery before iron, the `food + temporary shelter + stone` handoff, surface/scouting continuity after charcoal, the focused screenshot vs bridge mismatch, public speech provider 404s, sandboxed fetch EACCES, and detached-run persistence)`
- Hardcoded progression backbone removed from fallback path: `PASS`
- Long-run diamond/base/interior soak proven: `NOT YET`

## API Budget Safety Benchmark (2026-07-10)

| Area | Scenario | Status | Evidence |
| --- | --- | --- | --- |
| API budget safety | Official OpenAI daily soft stop halts gameplay without fallback continuation | PASS (unit) | UTC rotation, persistence, allowlist rejection, conservative missing-usage estimation (including HTTP errors), threshold events, one-shot blocking hooks, and pathfinder interruption are covered; a production-credential UTC rollover soak remains intentionally pending. |
